apiVersion: externaldns.k8s.io/v1alpha1
kind: DNSEndpoint
metadata:
  name: pronouns-global
spec:
  endpoints:
  - dnsName: pronouns.within.lgbt
    recordTTL: 3600
    recordType: CNAME
    targets:
    - aeacus.pronouns.within.lgbt